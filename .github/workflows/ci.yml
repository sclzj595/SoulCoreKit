name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-ubuntu:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.5.3'
          target: 'desktop'
          arch: 'gcc_64'
          modules: 'qtwebsockets'

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            g++ \
            lcov \
            libssl-dev

      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_TESTS=ON \
            -DENABLE_WARNINGS=ON \
            -DENABLE_LTO=ON

      - name: Build
        run: cmake --build build -j$(nproc)

      - name: Run tests
        run: |
          cd build
          ctest --output-on-failure

      - name: Build with coverage
        run: |
          cmake -S . -B build-coverage \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTS=ON \
            -DENABLE_COVERAGE=ON
          cmake --build build-coverage -j$(nproc)

      - name: Generate coverage report
        run: |
          cd build-coverage
          ctest
          lcov --directory . --capture --output-file coverage.info
          lcov --remove coverage.info '/usr/*' '*build-coverage/*' '*test*' --output-file coverage.info.cleaned
          genhtml -o coverage_report coverage.info.cleaned

  build-windows:
    runs-on: windows-2022  # ✅ 固定 Runner：windows-2022 同时预装 VS2022 + VC143/VC142 Toolset
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.5.3'   # ✅ 固定 Qt 6.5.3 LTS
          target: 'desktop'
          arch: 'win64_msvc2019_64'  # ✅ 固定 MSVC2019 ABI Qt，与 v142 Toolset ABI 一致
          modules: 'qtwebsockets'    # ✅ 不使用 --autodesktop，避免 aqt metadata 解析失败

      - name: Configure
        run: |
          # 工具链对齐：VS2022 Generator + v142 Toolset（MSVC2019） + x64
          cmake -S . -B build `
            -G "Visual Studio 17 2022" `
            -T v142 `
            -A x64 `
            -DCMAKE_BUILD_TYPE=Release `
            -DBUILD_TESTS=ON `
            -DENABLE_WARNINGS=ON

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Run tests
        run: |
          cd build
          ctest -C Release --output-on-failure

  build-macos:
    runs-on: macos-13  # ✅ 固定 macOS 13（Intel），避免 macos-latest 升级导致 AppleClang 版本漂移
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.5.3'
          target: 'desktop'
          arch: 'clang_64'
          modules: 'qtwebsockets'

      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_TESTS=ON \
            -DENABLE_WARNINGS=ON \
            -DENABLE_LTO=ON

      - name: Build
        run: cmake --build build -j$(sysctl -n hw.ncpu)

      - name: Run tests
        run: |
          cd build
          ctest --output-on-failure

  format-check:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format-14

      - name: Check formatting
        run: |
          find . \( -name "*.cpp" -o -name "*.h" \) -not -path "./build/*" -not -path "./.git/*" | xargs clang-format-14 --dry-run --Werror
